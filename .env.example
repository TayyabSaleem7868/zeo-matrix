# Local development environment variables
# Copy this file to `.env` and fill in values.
# Never commit `.env`.

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Admin Panel credentials (client-side gate)
# Note: this is not true security on its own (it ships to the browser). Use RLS + server-side checks too.
VITE_ADMIN_NAME=
VITE_ADMIN_SECRET=

# Admin Panel route (the URL path segment)
# Example: if set to `secret-admin-access-x9z`, the route becomes `/<value>`
VITE_ADMIN_ROUTE=secret-admin-access-x9z
